name: CI

on:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: macos-14

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run repository tests
        run: ./Scripts/test.sh
