{
  "id": "json_output",
  "title": "JSON Output",
  "description": "Return the result in a small JSON shape.",
  "body": "Return valid JSON only. Use exactly this schema: { \"text\": string }. The `text` value must contain only the final result text. If translation is active, `text` must contain only the final target-language result. Do not include extra keys such as `input`, `target`, `source`, or `language`.",
  "optionGroupIDs": ["strictness"]
}
